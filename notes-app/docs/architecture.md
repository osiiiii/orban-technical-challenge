FastAPI backend on 8000 with a sqlite file,Next frontend on 3000 that just calls it. The backend is split into layers so nothing does two jobs. All the queries sit in one file so search isn't a mess. The tags are a JSON array on the note row instead of their own table, keyword search runs in sql and the tag match in python after, fine at this size, and pulling tags into a real table is the one thing you'd change if it ever got big.
