API key in the X-API-Key header for everything under /notes.. health is open. GET /notes lists, POST creates, GET/PUT/DELETE /notes/{id} do the obvious, and /notes/search?q=&tag= searches title/body by q and exact tag by tag.Notes are title, body, tags, timestamps.errors all come back as {"detail": "..."}, 401/403 for the key, 404 missing, 422 bad input. Just run it and hit /docs for the real swagger.
