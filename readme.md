Two small apps for the Orban Labs backend dev tech challenge.

- **notes-app/** is a notes with tags, search, and API-key auth (FastAPI + Next.js).
- **url-shortener/** is coming next.

See each project's `docs/` for the plassnning and decisions behind it.